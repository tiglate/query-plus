# Legacy CSS (Phase 5+)

Source of truth is `ClientApp/src/styles/` (Tailwind 4 via Vite).

- Built output: `wwwroot/dist/css/site.css` (gitignored; built on publish / `pnpm run build`)
- `input.css` / `site.css` here are **deprecated** leftovers from the Tailwind 3 CLI era and are no longer linked from `_Layout`.
