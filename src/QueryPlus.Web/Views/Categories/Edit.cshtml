@model QueryPlus.Web.Controllers.Admin.CategoriesController.CategoryInputModel
@inject Microsoft.Extensions.Localization.IStringLocalizer<SharedResource> L
@{
    ViewData["Title"] = L["Categories_Edit"].Value;
}

<div class="qp-page space-y-4">
    <div class="qp-card">
        <div class="qp-card-header">
            <h1 class="qp-page-title">
                <i class="fa-solid fa-pen-to-square text-cyan" aria-hidden="true"></i>
                @L["Categories_Edit"]
            </h1>
            <div class="flex flex-wrap items-center justify-end gap-1.5">
                <button type="submit" form="category-form" class="qp-btn-primary">
                    <i class="fa-solid fa-floppy-disk" aria-hidden="true"></i>
                    @L["Save"]
                </button>
                <a asp-controller="Categories" asp-action="Details" asp-route-id="@Model.Id" class="qp-btn-secondary">
                    <i class="fa-solid fa-xmark" aria-hidden="true"></i>
                    @L["Cancel"]
                </a>
                <a asp-controller="Categories" asp-action="Index" class="qp-btn-secondary">
                    <i class="fa-solid fa-arrow-left" aria-hidden="true"></i>
                    @L["Back"]
                </a>
            </div>
        </div>
    </div>

    <div class="qp-card">
        <div class="qp-card-header">
            <h2 class="qp-section-title">
                <i class="fa-solid fa-folder" aria-hidden="true"></i>
                @L["Categories_Title"]
            </h2>
        </div>
        <div class="qp-card-body">
            <form id="category-form" method="post" asp-controller="Categories" asp-action="Edit" asp-route-id="@Model.Id">
                <input type="hidden" asp-for="Id" />
                <div class="flex flex-wrap items-start gap-4">
                    <div class="w-28 shrink-0">
                        <label class="qp-label">@L["Id"]</label>
                        <input class="qp-input" value="@Model.Id" readonly />
                    </div>
                    <div class="qp-category-description min-w-0">
                        <label class="qp-label qp-label-required" asp-for="Description">@L["Description"]</label>
                        <input class="qp-input" asp-for="Description" />
                        <span asp-validation-for="Description" class="qp-field-error"></span>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <div class="qp-card">
        <div class="qp-card-header">
            <h2 class="qp-section-title">
                <i class="fa-solid fa-clock-rotate-left" aria-hidden="true"></i>
                @L["Audit"]
            </h2>
        </div>
        <div class="qp-card-body">
            <div class="grid max-w-3xl grid-cols-2 gap-4 text-sm">
                <div><span class="text-slate-500">@L["CreatedAt"]</span><div>((DateTime)ViewBag.CreatedAt).ToLocalTime().ToString("g")</div></div>
                <div><span class="text-slate-500">@L["UpdatedAt"]</span><div>@(((DateTime?)ViewBag.UpdatedAt)?.ToLocalTime().ToString("g") ?? "—")</div></div>
            </div>
        </div>
    </div>
</div>
