namespace QueryPlus.Domain.Enums;

/// <summary>
/// Values of tb_revision_type.id_revision_type (TINYINT).
/// </summary>
public enum RevisionTypeCode : byte
{
    Insert = 1,
    Update = 2,
    Delete = 3
}
